# 调研洞察汇报助手 · v0 设计文档（结构 + 数据 Schema + Rubric）

> 本文档面向 OpenHarness 平台里的**调研洞察汇报助手**产品。它和已有的「业务汇报助手」（经营月报、算数字型）是**两类任务**，schema / rubric / judge 逻辑都不同。本文档按已有两份母文档（`业务汇报助手_v0结构设计文档.md`、`业务汇报助手_Rubric落地文档.md`）的体例编写，可直接对接 harness/app。
>
> **本文只做设计，不改代码。** §7 单列「落地时代码需要改什么」，供后续实现参考。

---

## 0. 开始之前：三条铁律（沿用平台共识）

1. **结构定质量上限**：flow / subagent / memory schema 由人在 v0 设对，MVP 里 Optimizer 只动 instructions / few_shots / memory_content，**不动结构**。
2. **可回溯性是这个产品的生命线**：调研报告没有唯一正确答案，但"每个论断能否落到某段素材"是可客观评的。整个结构围绕这一点设计。
3. **judge 没校准到 ≥0.85 一致率，不许开 Optimizer**——尤其洞察维度最主观，校准更关键。

---

## 1. 这个产品到底在做什么

**核心任务**：输入多份异构调研素材（doc / pdf / excel / 访谈纪要 / 数据表）→ 输出一份**有据、有洞察、面向高管**的调研报告（可编辑 doc）。

真实场景举例（你的两个近期项目）：
- **DeepSeek 用户使用时长分析** —— 输入：DAU/时长明细 Excel + 用户访谈纪要 + 竞品对比 doc；输出：说清"时长为何变化、谁在贡献、下一步动作"的高管报告。
- **AI Native 公司 Agent 落地分析** —— 输入：多家公司案例 doc + 访谈 + 公开资料；输出：提炼落地模式、风险、给我们的启示。

它和经营月报最大的区别：

| | 经营月报（业务汇报助手） | 调研洞察汇报助手（本产品） |
|---|---|---|
| 输入 | 一张结构化指标表 | 多份 doc/pdf/excel 异构素材 |
| "正确"的锚 | 算得对（数字可重算） | **有据可回溯 + 不编造 + 洞察非平凡** |
| ground truth | 预算好的数字 findings | 素材支持哪些结论 + 期望达到的洞察 |
| 主要失分点 | 编造数字、口径错 | **编造素材没有的事实、结论无出处、素材冲突不指出、过度外推** |
| 交付物 | 一页报告 | 可编辑 doc（评估时按文本评） |

它的质量上限由四件事决定，**整个结构就是围绕这四件事设计的**：

| # | 质量维度 | 说明 | 特点 |
|---|---------|------|------|
| 1 | **论据可回溯性** | 每个论断都能落到某段素材，且不编造、不曲解 | 最容易崩，最需要结构兜底（红线维度） |
| 2 | **覆盖度** | 关键结论无遗漏、required_sections 齐 | 结构化，好托底 |
| 3 | **洞察质量** | 有非平凡判断（归因/趋势/建议），不是素材复述 | 这个产品的核心价值，judge 最难判 |
| 4 | **受众契合度** | 面向高管精炼、直击决策；不堆术语、不注水 | 反向维度，防 reward hacking |

---

# Part A · v0 结构

## 2.1 执行流程（Flow）

**关键设计原则**：把"读素材/抽事实"和"下判断/写报告"彻底分开。写作阶段**只能引用已抽取、带出处的素材片段与已确认的结论**，无权凭空引入新事实。这和业务汇报助手"算数据 vs 写报告分离"是同构的——只是这里分离的是"**证据 vs 论断**"。

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 0  Intake & Scoping                                          │
│  确定：报告主题 / 受众 / 交付形式 / 调研问题(要回答什么)             │
│  产出：report_spec { topic, audience, key_questions,              │
│                      required_sections, deliverable }             │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1  Source Ingestion & Chunking     [Source Curator subagent]│
│  解析 doc/pdf/excel → 切片 → 每片配唯一 ID(S-001…) + 元数据         │
│  ★只做"抽取+切片+标ID",不做判断★                                   │
│  产出：sources[]  每条带 { id, type, title, excerpt, meta }       │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2  Evidence & Claim Extraction      [Analyst subagent]      │
│  从 sources 里抽"素材直接支持的事实/结论",逐条挂引用              │
│  识别素材间的冲突、缺口、口径差异,显式标出                          │
│  ★每条 claim 必须带 source_ids;不能引入 sources 外的事实★          │
│  产出：claims[] { id, claim, source_ids[], type, conflict? }      │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3  Insight Synthesis                [Insight subagent]      │
│  在 claims 之上做非平凡判断:归因 / 趋势 / 对我们的启示 / 建议       │
│  ★判断必须 depends_on 某些 claim;区分"素材支持"与"我的推断"★       │
│  产出：insights[] { insight, depends_on:[claim_id], confidence }  │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4  Narrative Composition            [Writer subagent]       │
│  按 memory 模板把 claims + insights 组织成高管 doc                 │
│  每个论断携带 [S-xxx] 或 [C-xxx] 引用标记                          │
│  产出：draft_report (markdown, 后续渲染为可编辑 doc)               │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5  Verification（独立、对抗性）      [Verifier subagent]      │
│  逐句检查:① 每个论断能回溯到 source/claim 吗?(抓编造/曲解)         │
│           ② key_questions 都回答了吗?关键 claim 漏没漏?(抓遗漏)     │
│           ③ 素材冲突有没有被无视?有没有过度外推?(抓越界)            │
│  产出：verification_report { pass/fail, 逐条问题 }                 │
└───────────────────────────────┬─────────────────────────────────┘
                        ┌────────┴────────┐
                    pass │            fail │（退回 Step 4 重写，最多 N 次）
                        ▼                  ▼
┌─────────────────────────────────┐   回到 Writer，带着问题重写
│ STEP 6  Format & Deliver          │
│  按高管受众套格式 → 输出可编辑 doc  │
└─────────────────────────────────┘
```

**为什么这个流程决定上限：**

- **Step 1 素材切片 + Step 2 挂引用**：从架构上封死"编造素材里没有的事实"这条最大失分路径——写作层拿不到"自由编事实"的权力，一切论断都要挂 `source_ids`。这是可回溯性维度的天花板保障。
- **Step 2 显式标冲突/缺口**：调研素材天然有矛盾（如两份数据口径不同），把"识别冲突"变成一个**强制产出字段**，才能让报告"指出冲突"而非"悄悄混用"。这是这个产品区别于经营月报的关键设计。
- **Step 3 区分"素材支持 vs 我的推断"**：洞察必须 `depends_on` claim，且标 confidence——这既是洞察质量的抓手，也防止"过度外推"污染可回溯性。
- **Step 5 独立对抗性验证**：**必须是独立上下文 + 对抗性**（任务是"挑错"而非"确认"），否则形同虚设。

## 2.2 Subagent 拆分

拆分依据是**职责隔离 + 上下文聚焦 + 让验证独立**。

| Subagent | 单一职责 | 为什么单独拆（抬上限的理由） |
|----------|---------|---------------------------|
| **Source Curator** | 解析/切片素材、配 ID 和元数据，输出结构化 sources | 把"读原始文件"和"下判断"隔离；上下文只装素材，防止边读边编 |
| **Analyst** | 抽素材直接支持的事实/结论、标冲突缺口、挂引用 | "素材说了什么"是客观抽取，与"我判断什么"是不同能力，分开才防越界 |
| **Insight** | 归因/趋势/启示/建议，只在 claims 内推理 | 洞察是这个产品的核心价值，需独立上下文专注做非平凡判断 |
| **Writer** | 组织语言、套高管模板，携带引用标记 | 写作上下文只关心表达，不被抽取细节干扰 |
| **Verifier**（独立） | 对抗性查错（编造/曲解/遗漏/过度外推），不参与生成 | **★最关键★** 独立上下文才能真正挑错；可回溯性的兜底 |

**v0 取舍**：先做这 5 个。素材解析（PDF/Excel → 文本）在 v0 可先当 Source Curator 的内部步骤，甚至先人工把关键片段填进 `excerpt`（见 §4）。要不要把解析单独拆成工具/subagent，等 Optimizer 撞平台期、失败信号指向"解析丢信息"时再说。

## 2.3 Memory Schema

```yaml
memory:

  # ① 配置型 —— 稳定，人工/初始化写入，决定"报告长什么样"
  config:
    report_templates:
      research_insight:
        required_sections: [背景与目的, 核心发现, 归因分析, 趋势判断, 对我们的启示/建议]
        audience: exec
        tone: concise
    audience_profiles:
      exec: { length: short, focus: [结论, 风险, 决策项], detail_level: low }
      analyst: { length: long, detail_level: high }
    source_type_hints:            # 不同素材类型的抽取提示
      excel: "优先抽指标口径、时间范围、样本量"
      pdf: "抽核心论点+页码;区分作者观点与引用数据"
      interview: "区分受访者主张与事实;标注是几人提及"

  # ② 事实型 —— 累积型,跨报告复用的领域知识
  facts:
    domain_baselines:             # 领域基线,支撑"这个数字算高还是低"
      - { topic: "AI产品使用时长", ref: "行业人均~X min", source_ref: ... }
    prior_reports: [ 指向历史调研报告的引用 ]

  # ③ 习得型 —— 反馈/优化写入,Optimizer 和人工纠正的落脚点
  learned_rules:
    - rule: "涉及趋势判断时,若素材无连续多期数据,必须标注'外推,置信度低'"
      origin: optimizer 从'过度外推'失败模式提炼
    - rule: "两个数据源口径不一致时,报告须先声明口径再比较"
      origin: 人工反馈
```

**为什么这样分层直接关系到上限和优化闭环：**
- **config** 决定个性化上限——模板、受众偏好、素材抽取提示定得越准，输出越贴需求。**人工资产。**
- **facts** 决定"能不能判断量级"——没有领域基线，"27 分钟算高吗"无从判断，洞察封顶。
- **learned_rules** 是**优化闭环的物理落点**。调研场景最容易积累的规则就是"哪些坑要避"（外推、口径、单一信源），沉淀在这里让平台越用越好。

## 2.4 结构 ↔ 四维度自检表

| 质量维度 | 结构对应物 |
|---------|-----------|
| **论据可回溯性** | Step1 切片配ID + Step2 强制挂 source_ids + Step5 独立验证（最强保障） |
| **覆盖度** | `report_spec.key_questions` + `required_sections` + Verifier 逐项核对 |
| **洞察质量** | Insight subagent 专职 + `depends_on` 约束 + facts 领域基线 |
| **受众契合度** | `audience_profiles` 控长度 + 作为反向维度防注水 |

四个维度都有明确结构对应物 → v0 结构没有留下"再怎么调 prompt 也够不到"的盲区。

---

# Part B · 数据 Schema（你要产出的东西）

> 这是你作为专家**不能外包**的核心资产。每条 case 一行 JSON（dataset.jsonl），成本全在 `ground_truth`——你要预先把"素材支持哪些结论、期望达到哪些洞察、埋了哪些坑"标出来。

## 3.1 dataset.jsonl 单条 case 格式

```jsonc
{
  "case_id": "rr-001",
  "report_type": "research_insight",
  "topic": "DeepSeek 用户使用时长分析",
  "audience": "exec",
  "deliverable": "editable_doc",
  "key_questions": [                        // 这份报告必须回答的调研问题
    "使用时长为何变化？",
    "增长由哪类用户贡献？",
    "下一步动作建议？"
  ],
  "required_sections": ["背景与目的","核心发现","归因分析","趋势判断","对我们的启示/建议"],

  "input": {                                // ← 喂给 skill 的原始素材(抽取后的文本片段)
    "sources": [
      {"id":"S-001","type":"excel","title":"Q2 DAU与时长明细",
       "excerpt":"人均日时长: Q1=18min, Q2=27min; 重度用户(前20%)时长+62%, 轻度用户+3%; 样本 N=12k",
       "meta":{"period":"2026Q1-Q2","source_system":"内部BI"}},
      {"id":"S-002","type":"interview","title":"重度用户访谈纪要(6人)",
       "excerpt":"6人中5人提到把DeepSeek当日常工作台,替代了部分搜索和文档工具",
       "meta":{"n_interviewees":6}},
      {"id":"S-003","type":"doc","title":"竞品X时长口径说明",
       "excerpt":"竞品X披露的时长为'去重会话时长',与S-001的'累计前台时长'口径不同",
       "meta":{}}
    ],
    "brief": "面向高管,说明时长变化原因和下一步动作,不超过1.5页"
  },

  "ground_truth": {                         // ← 你(专家)预先标注的"标准答案"
    "supported_claims": [                   // 素材真正支持的结论 + 引哪几段
      {"id":"C-001","claim":"人均日时长 Q1→Q2 从18升至27分钟(+50%)","source_ids":["S-001"],"type":"fact"},
      {"id":"C-002","claim":"增长主要由重度用户贡献,轻度用户几乎持平","source_ids":["S-001","S-002"],"type":"attribution"}
    ],
    "key_claim_ids": ["C-001","C-002"],     // 漏了就扣"覆盖度"

    "expected_insights": [                  // 好分析师应达到的非平凡判断(洞察维度锚)
      {"id":"I-001","insight":"增长来自留存加深而非拉新,获客策略应转向激活与深度","depends_on":["C-002"]}
    ],

    "traps": [                              // 故意埋的坑(硬case),测skill会不会犯错
      {"type":"source_conflict","involves":["S-001","S-003"],
       "detail":"S-001与竞品S-003时长口径不同,好报告应指出不可直接比较,而非混用"},
      {"type":"unsupported_extrapolation","involves":["S-001"],
       "detail":"素材只有两期数据,不能断言'时长将持续上升'"}
    ]
  },

  "hard_case_tags": ["source_conflict","unsupported_extrapolation"],
  "split": "dev"                            // train / dev / test
}
```

**字段说明与理解要点：**

| 字段 | 作用 | 谁来填 |
|------|------|--------|
| `input.sources[].excerpt` | 素材抽取后的**文本片段**（eval 跑的是文本，不是原始二进制） | v0 手工填关键片段；后续由 Source Curator 自动生成 |
| `input.sources[].id` | 每片素材唯一 ID（S-xxx），是可回溯性的锚 | 系统/你 |
| `ground_truth.supported_claims` | 素材**直接支持**的结论 + 引用段 | **专家（你）** |
| `key_claim_ids` | 其中"漏了就扣覆盖度"的关键结论 | **专家** |
| `expected_insights` | 好报告应达到的非平凡判断 | **专家** |
| `traps` | 故意埋的坑，测 skill 会不会犯错 | **专家** |
| `hard_case_tags` | 坑的类型标签，用于失败聚类 | 由 traps 派生 |

## 3.2 硬 case 类型（调研场景专属的"硬骨头"）

数据集里**必须**包含这些坑，否则优化出的 skill 一上线就崩：

| tag | 含义 | 测什么 |
|-----|------|--------|
| `source_conflict` | 两份素材数据/口径/结论冲突 | 会不会指出冲突，而非悄悄混用 |
| `unsupported_extrapolation` | 素材不足以支撑某种外推 | 会不会过度外推、乱下趋势结论 |
| `single_source_overweight` | 结论只有单一信源（如仅1人访谈） | 会不会把孤证当普遍规律 |
| `missing_evidence` | 关键问题素材里根本没答案 | 会不会诚实说"素材不足"，而非编造 |
| `stale_or_scope_mismatch` | 素材时间/范围与调研问题不匹配 | 会不会误用不相关素材 |
| `buzzword_bait` | 素材里有大词但无实质 | 会不会被术语带偏、复述空话 |

## 3.3 human_labels.jsonl 格式（校准集，不变）

格式和业务汇报助手完全一致——只是维度名换成本产品的四维：

```jsonc
{"case_id":"rr-001","split":"dev",
 "human_scores":{"traceability":4,"coverage":3,"insight":2,"audience_fit":3},
 "labeler":"你的名字",
 "_note":"对某版输出的专家打分;真实项目由你手工填"}
```

- 抽 **30–50 条** 覆盖高/中/低质量，由你按 §5 rubric 人工打分。
- 用途：校准 judge（一致率 ≥ 0.85 才算 judge 可信）。**这一步不做，后面全是空中楼阁。**

---

# Part C · Rubric

## 4. 维度总览

| # | 维度 | 一句话判据 | 对应结构环节 | 权重 | 类型 |
|---|------|-----------|-------------|------|------|
| 1 | **论据可回溯性** | 每个论断可落到 source/claim，无编造、无曲解 | Step1切片 + Step2挂引用 + Step5验证 | **0.40** | 主维度（最易崩，红线） |
| 2 | **覆盖度** | key_questions 全回答、key_claim 无遗漏、段落齐 | report_spec + Verifier | 0.20 | 结构化（好托底） |
| 3 | **洞察质量** | 命中 expected_insights，有归因/趋势/建议、非复述 | Insight subagent + facts | **0.30** | 核心价值、最难判 |
| 4 | **受众契合度** | 高管精炼、直击决策、不注水堆术语 | audience_profiles | 0.10 | **反向维度（防 hack）** |

> 权重设计意图：可回溯性 0.40（生命线，最易崩）；洞察质量给到 0.30（这个产品的核心价值，比经营月报更看重）；受众契合 0.10 但**必须存在**——堵住"靠堆砌大词/凑篇幅骗高分"这条路。

## 5. 各维度评分标准（1–5 锚点 + 正反例）

> 打分约定：整数 1–5。**命中最严重扣分项就落在对应档，不因其他方面好而回补**（可回溯性尤其——编造一条素材没有的事实，不能因为文笔好给高分）。

### 维度 1 · 论据可回溯性（权重 0.40）

**判据**：报告中每一个事实、数据、结论，都①能回溯到某个 source 片段或已确认 claim，②未曲解素材原意，③没有素材里不存在的"新事实"，④对素材间冲突有正确处理（指出而非混用）。

| 分 | 锚点 |
|----|------|
| **5** | 所有论断均可回溯到 source/claim；无曲解、无编造；素材冲突被正确指出。 |
| **4** | 可回溯性完整；仅 1 处非关键引用缺失或表述略模糊，不影响结论。 |
| **3** | 主体可回溯，但有 1 处**关键论断无出处**，或 1 处**轻度曲解**素材。 |
| **2** | 出现 ≥1 处**编造事实**（素材里不存在）或**把冲突素材当一致直接混用**，核心可信度受损。 |
| **1** | 大量论断无法回溯或明显编造；报告的事实基础整体不可信。 |

- ✅ **正例**："人均日时长 Q2 达 27 分钟，较 Q1 +50%[S-001]，增长主要来自重度用户（时长 +62%，轻度仅 +3%）[S-001]，访谈亦印证重度用户将其作为日常工作台[S-002]。注：竞品 X 披露口径为去重会话时长[S-003]，与本数据不可直接比较。"
- ❌ **反例（编造）**："使用时长大幅提升，用户满意度也随之显著上升，NPS 预计突破 50。" —— "满意度""NPS"素材里都没有（编造）→ **2 分封顶**，即便文笔流畅。
- ❌ **反例（混用冲突）**：直接写"我们时长 27min 已超竞品 X 的 20min" —— 无视 S-003 口径不同 → 落 2 分。

**区分度检查**：4 vs 3 = 缺的是关键论断还是无关标记；3 vs 2 = 引用瑕疵/轻度曲解 vs 凭空编造/混用冲突。**编造 = 2 分封顶，硬红线。**

### 维度 2 · 覆盖度（权重 0.20）

**判据**：`key_questions` 全部得到回答；`key_claim_ids` 无遗漏；`required_sections` 全覆盖且非空。

| 分 | 锚点 |
|----|------|
| **5** | key_questions 全答、key_claim 无遗漏、段落齐且有实质内容。 |
| **4** | 段落齐、问题全答；有 1 个次要 claim 未提，不影响判断。 |
| **3** | 缺 1 个 required section，**或**漏 1 个关键 claim / 未答 1 个 key_question。 |
| **2** | 缺 ≥2 段，或漏多个关键 claim / 多个问题未答，完整到影响决策。 |
| **1** | 大面积缺失，报告不成形。 |

- ✅ **正例**：五段齐全，三个 key_question 都有明确回答，C-001/C-002 都写入。
- ❌ **反例**：只答了"时长为何变化"，漏答"下一步动作"，且缺"对我们的启示"段 → 落 2 分。

**区分度检查**：只看"该有的有没有、关键的漏没漏"，**不评价写得好不好**（那是洞察/受众维度的事）。

### 维度 3 · 洞察质量（权重 0.30）

**判据**：报告在陈述素材之外，提供**基于 claims 的非平凡判断**——至少含：①**归因**（为什么，且有 claim 支撑），②**趋势/风险判断**（且标明置信度、不过度外推），③可执行的**启示/建议**。判断须挂在 claims 上，命中 `expected_insights` 视为达标。

| 分 | 锚点 |
|----|------|
| **5** | 命中 expected_insights；归因具体有据、趋势判断标明置信度、建议可执行；提供了需综合多条 claim 才得出的非平凡判断。 |
| **4** | 三要素齐，但其中 1 项较泛或支撑略弱，或漏 1 条 expected_insight。 |
| **3** | 有判断但偏表面（只说"增长了"不解释为何，建议空泛如"继续关注"）。 |
| **2** | 基本是**素材复述**，几乎无归因/趋势/建议。 |
| **1** | 纯素材罗列，无任何判断。 |

- ✅ **正例**："增长来自留存加深而非拉新（重度 +62% vs 轻度 +3%[S-001]，访谈印证工作台化[S-002]），意味着获客策略应从'拉新'转向'激活与深度使用'；但仅两期数据，'趋势将持续'为低置信外推，建议下季度补齐连续数据再定论。" —— 归因、趋势（标置信度）、建议俱全，且都挂 claim。
- ❌ **反例（复述）**："用户使用时长增长了，用户很喜欢我们的产品，建议继续保持。" —— 无归因、无风险、建议空泛 → 落 2 分。
- ❌ **反例（越界发挥）**："时长增长说明我们已建立不可撼动的护城河。" —— "护城河"无素材支撑 → 洞察落低分，**同时拉低可回溯性**（无据结论）。

**区分度检查**：5 vs 4 = 是否有需综合多条 claim 的非平凡判断 + 是否命中 expected_insights；3 vs 2 = 三要素是否有实质内容。**越界发挥跨维度扣分**（洞察 + 可回溯性双降），防"编个漂亮判断"骗洞察分。

### 维度 4 · 受众契合度（权重 0.10，反向维度）

**判据**：面向高管——精炼、结论先行、直击决策项；无冗余、不堆术语、长度匹配 `audience_profiles`。**这是防 reward hacking 的反向闸门。**

| 分 | 锚点 |
|----|------|
| **5** | 结论先行、直击决策；无冗余；长度匹配 exec 设定（≤1.5 页）。 |
| **4** | 基本精炼，有个别可删句。 |
| **3** | 一定冗余或轻度术语堆砌，或长度偏离受众。 |
| **2** | 明显冗长/套话术语堆砌（"赋能""闭环""范式"式堆词），或严重偏离长度。 |
| **1** | 充斥空话，信息密度极低。 |

- ✅ **正例**（exec）：开头一句结论，随后 3 段支撑 + 决策项，约 1 页。
- ❌ **反例（典型 hack 症状）**："本报告基于多维度、全链路的深度洞察，构建了对用户行为范式的系统性认知框架……" —— 满是大词、信息量趋零 → 落 2 分。**这正是 Optimizer 可能学出的讨好行为，本维度就是用来把它拽下来的。**

**区分度检查**：只惩罚"多而空"，**不奖励"短"**——为短而漏内容，扣的是覆盖度，不在这里加分。

## 6. 打分门槛与 gate（供 Optimizer 验证环节使用）

- **红线一票否决**：论据可回溯性 < 3（出现编造/混用冲突）→ 该 case 直接不合格，无论总分多高。
- **多目标不许拆东墙**：候选版本采纳条件 = "目标维度涨、且其它维度都不塌"（任一维度较上版跌超阈值即否决），**分维度看，不看总分平均**。
- **反向维度守门**：受众契合度 < 3 触发人工复检（大概率是 Optimizer 开始靠堆砌骗分）。

**目标阈值（v0 初始，拿到首批标注后校准）：**

```
论据可回溯性: >=4.2    覆盖度: >=4.0    洞察质量: >=3.6
受众契合度: >=3.5      overall: >=4.0   judge_human_agreement: >=0.85
```

> 洞察质量目标定 3.6（比经营月报还低）是刻意的——它最主观最难，硬拉高反而诱导 hack；靠 gate 守住可回溯性和防注水，让洞察自然爬升。

---

# Part D · 落地相关

## 7. 落地时代码需要改什么（供后续实现参考，本次不做）

换数据集**不够**——现有 harness 是为"算数字型"写死的，切到调研洞察需要改这几处：

| 模块 | 现状 | 需改成 |
|------|------|--------|
| `harness/schemas.py` | case 用 `ground_truth_findings`（数字+出处） | 换成本文 §3.1 的 `sources` + `ground_truth{supported_claims, expected_insights, traps}` |
| `harness/judge.py` | 按"数字重算 + 对出处"打分 | 改为"论断对 source/claim 的可回溯性检查 + trap 命中检查 + expected_insight 命中"打分 |
| `harness/backend.py`（MockBackend） | 按 directives 输出数字型报告 | 换成按 directives 输出"引用素材片段/编造/指出冲突"等行为，才能让 mock 演示闭环 |
| `harness/optimizer.py` | directives 针对数字场景（require_citation 等） | 新增调研场景 directive：`require_source_ref` / `flag_source_conflict` / `mark_extrapolation_confidence` / `require_insight_triplet` / `match_exec_length` 等 |
| rubric artifact | 四维 = 数据准确性/完整性/洞察/简洁 | 换成本文四维 + 新锚点 |
| `app/generator.py` | 识别报告类型=经营月报等 | 增加 `research_insight` 类型识别 + 本产品 rubric 模板 |

**Phase0 建议路径**：先照 §3.1 手工做 **3–5 条** 真实 case（DeepSeek 用 2 条、AI Native 用 2–3 条，至少各含 1 个 trap），把 `excerpt` 填成你手头素材的真实关键片段；再改 schema/judge/mock，跑通离线闭环；最后接真实 Claude backend 处理 PDF/Excel 解析。

## 8. v0 验收清单（Definition of Done）

- [ ] flow 6 步、5 个 subagent、memory 三层都已明确，且每个质量维度都能在 §2.4 表里找到结构对应物
- [ ] dataset ≥ 3 条真实 case，覆盖 DeepSeek + AI Native 两主题，含 ≥3 个不同 trap 类型
- [ ] 每条 case 的 `ground_truth`（supported_claims / key_claim_ids / expected_insights / traps）由你人工标注完成
- [ ] human_labels 抽 30–50 条（或 v0 先小样）由你按 §5 rubric 打分
- [ ] judge 校准一致率 ≥ 0.85（否则先改 rubric 锚点，不动 skill）
- [ ] 四维 rubric 权重/锚点/gate/目标阈值确认
- [ ] 明确 §7 的代码改动范围，再进入实现

---

## 附录 · 与业务汇报助手的映射（复用性说明）

本产品和业务汇报助手是**同构**的——只是把"数字"换成"论断"、"finding"换成"claim/source"：

| 业务汇报助手 | 调研洞察汇报助手 |
|------|------|
| findings（算好的数字+出处） | claims（素材支持的结论+source_ids） |
| 数据准确性（数字可回溯） | 论据可回溯性（论断可回溯） |
| 编造数字 = 红线 | 编造事实 / 混用冲突 = 红线 |
| Data Analyst（算数） | Source Curator + Analyst（读素材、抽claim） |
| 硬case：缺口/异常/口径混/矛盾 | 硬case：冲突/过度外推/孤证/素材不足 |

所以结构设计模板、rubric 编写方法论、judge 校准流程**全部可沿用母文档**，本文档只是这套方法论在"调研洞察"场景的一次实例化。
