# OpenHarness 20260727 系统更新

本轮更新完善了 OpenHarness 基于 WB CLI 的真实报告生成、批量 Judge 和
Skill 迭代闭环，集中解决以下 5 个问题。

## 1. 生成与评测数据格式不统一

### 问题

WB CLI 与 OpenHarness 使用不同数据结构，需要额外转换和 ID 映射，容易造成
case、报告和评测结果错位。

### 修改

- 统一使用 `openharness-wb/v1`，同一个 `data.json` 同时服务报告生成和评测。
- 使用统一 `case_id` 关联 Session、WB 结果、报告和 Judge。
- 保留 `ground_truth`：不发送给 WB 生成模型，只发送给 Judge。
- 素材路径相对 `data.json` 解析，并在任务启动前校验存在性。
- 任务记录数据集 SHA256，防止运行期间数据变化后错误导入。
- 当前正式测试数据为 `10_real_project` 与 `10_real_project_new`，共 20 条。

## 2. 批量 Judge 与下一版 Skill 状态流不连贯

### 问题

批量 Judge 完成后，下一版 Skill 按钮可能仍不可用；Judge 发现的
`partial/miss` 也可能没有稳定传入 Optimizer。

### 修改

- 一次批量 Judge 当前版本全部 case，每个 case 使用独立 Prompt。
- case 级 Judge 结果即时持久化，失败或中断后只继续未完成 case。
- 后端统一计算 `version_status` 和 `actions`，前端只按后端状态启用按钮。
- 全部报告到齐后才允许 Judge；全部 Judge 完成后才允许生成下一版 Skill。
- 真实 Judge checks 直接生成 failed patterns，并持久化为 `failure_report`。
- Optimizer 只读取当前版本的真实 `failure_report`。
- 未映射的失败 check 会阻断推进，不再被误判为收敛。
- Judge 写入前校验报告、Rubric 和 Skill 版本，防止旧结果污染当前版本。

## 3. Session Skill 与 WB 实际执行内容不一致

### 问题

任务虽然记录了 `skill_version=vN`，但 WB CLI 可能仍执行固定基础 Skill，
无法保证每轮实验真正使用当前 Session 版本。

### 修改

- 启动任务时将当前 `SkillArtifact` 编译为完整、不可变的 Skill 目录。
- 冻结目录包含 `SKILL.md`、`references/instructions.md` 和
  `references/session_artifact.json`。
- Generation Job 记录 Session、版本、artifact hash、目录 hash 和 compiler 版本。
- WB CLI 只执行任务冻结的 Skill 副本，不回退到固定基础目录。
- 报告导入前再次校验冻结目录 hash；内容被修改时拒绝自动导入。
- Rubric、ground truth、Judge 结果不会进入 WB 执行 Skill。

## 4. WB 单 case 重试与运行配置不完善

### 问题

旧逻辑需要等待整批 case 完成后再重试失败 case，降低并发利用率；长 case ID
还可能导致 WorkBuddy 原生路径超过 macOS 限制。

### 修改

- case 未产出有效报告时立即重试，不等待同批其他 case。
- 默认最多额外重试 3 次，即每个任务最多尝试 4 次。
- 重试耗尽后，可继续创建“仅重试失败 case”任务，没有跨任务总次数上限。
- 报告一旦通过 Artifact Validator，立即停止重试并只导入一次。
- Web UI 可配置报告生成模型、报告生成并发和 Judge 并发。
- 默认报告模型为 `deepseek-v4-pro-ioa`，默认生成/Judge 并发均为 20。
- 当前不设置人为并发安全上限。
- 新任务和失败 case 重试均可单独指定模型与并发。
- 内部 run ID 和 WB Session ID 改用固定长度哈希，修复
  `ENAMETOOLONG`，业务 `case_id` 保持不变。

## 5. 生成文件与 trace 严重冗余

### 问题

每个 attempt 都保留完整 workspace 和重复 stdout；逐 token
`stream_event` 占据大部分 trace，导致运行目录快速膨胀。

### 修改

- 每个 attempt 先捕获报告、artifact manifest 和诊断信息，再删除临时
  `trace/workspace`。
- 不再生成 `trace/rounds/*/stdout.jsonl`。
- 保留聚合后的 `trace/2_events.jsonl`，但过滤逐 token
  `stream_event`。
- `stream_event` 仍作为运行期心跳参与 stall timeout 判断，只是不再落盘。
- 继续保留 request、result、stderr、operations、artifact manifest 和最终报告。
- 清理逻辑同时覆盖成功和失败 case，不影响报告验收与自动导入。

## 验证

- Python 3.9：App 28/28、Harness 31/31。
- Python 3.13：App 28/28、Harness 31/31。
- 前端 JavaScript 语法检查通过。
- 20-case 数据完整性和生成/Judge状态流冒烟测试通过。
- 超长 case ID、即时重试、模型/并发覆盖和运行产物清理均有回归测试。

## 本地运行注意事项

- `data/`、Session 实验数据和 `generation_runs/` 仅用于本地，不提交 GitHub。
- 代码更新后需要重启 Web server 才会生效。
- 运行中的任务应先等待结束再重启，避免被恢复为 `interrupted`。
- 本轮存储清理策略只影响新任务，不自动删除历史实验目录。
