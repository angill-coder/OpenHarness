{
  "name": "report-agent-v4",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p '*_test.py' && python3 scripts/check_judge_model.py",
    "build": "node scripts/build.mjs"
  },
  "description": "report-agent-v4: WorkBuddy native Team expert for research report writing, dynamic-dimension judging, and markdown writing memory"
}
