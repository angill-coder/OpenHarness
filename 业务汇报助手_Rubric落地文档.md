# 业务汇报助手 · Rubric 落地文档（兼「Rubric 编写方法论」）

> **文档定位**：本文件是业务汇报助手的**评分标准（rubric）**，也是平台优化闭环里 Judge 的判据源头。它同时承担两个角色：
> 1. **业务汇报助手的可用 rubric**：4 个维度、每维度 1–5 分档锚点、正反例、区分度检查、聚合与阈值——可直接填入平台的 `rubric` schema，供 Judge 打分、Optimizer 优化。
> 2. **Rubric 编写方法论**：把"怎样写出一份有区分度、不被 hack 的 rubric"抽象成可复用的流程 + 检查清单，供另外 3 个产品套用。
>
> **为什么这份文档最重要**：rubric 是**决定平台成败的杠杆**。结构定上限，但 rubric 定义"什么是好"——rubric 写不好，Optimizer 会忠实地把 skill 优化到**讨好裁判（reward hacking）**上，分数涨了、真实质量没涨甚至下降。这是唯一一份"写砸了会让整个平台变成噪声放大器"的文档。
>
> **配套阅读**：`业务汇报助手_v0结构设计文档.md`（本 rubric 的四维度与那份文档的四维度一一对应）。
>
> 维护者：平台/架构负责人 + 业务专家 · 版本：v0 · 最后更新：2026-07-05

---

## 0. 开始之前：一份好 rubric 的 5 条标准

写任何一个维度前，先记住合格线。一个维度的定义，必须同时满足：

1. **可判定**：判据是"能不能指着 trace 说清扣分点"，不是"感觉好不好"。禁止出现"高质量""专业""清晰"这种无法核对的词，除非后面立刻跟上"体现为……"的可观察表现。
2. **有锚点**：1–5 每一档都有**具体的、可区分的**描述（level anchor）。相邻两档的差别要能让两个不同的人读完后打出同一个分。
3. **带正反例**：每个维度至少 1 正例 + 1 反例。**反例比正例更重要**——它是区分度的来源，也是防 hack 的堵漏点。
4. **可回溯到结构**：这个维度的高分/低分，必须能对应到 v0 结构里的某个环节（否则要么结构有盲区，要么这个维度没法优化）。
5. **抗 hack**：设想"一个只想骗高分、不想真做好的模型会怎么钻空子"，把那条路在判据里堵掉。至少有一个**反向维度**（这里是简洁性）专门干这件事。

---

## 1. 这份 rubric 在平台里怎么被使用

```
skill 跑出 trace（含完整执行过程 + 最终报告）
        │
        ▼
   Judge（LLM-as-judge）读【rubric + trace】→ 逐维度打分 + 打分理由
        │
        ├─▶ 分维度 scores 进 Eval Record
        │
        ├─▶ 低分维度 = 失败聚类的抓手 → Optimizer 的优化目标
        │
        └─▶ 抽样 vs 人工标注 → meta-eval 一致率（校准 judge，见 §6）
```

三个要点，决定 rubric 怎么写：

- **分维度打分，不给单一总分让 judge 自由发挥**。总分由 §5 的加权公式**机械算出**。否则 judge 会把"整体印象"糊成一个数，Optimizer 无从下手，也会牺牲难维度换易维度抬平均分。
- **每个分数必须附带"打分理由 + 证据定位"**。理由是给失败聚类和 Optimizer 读的，也是 §6 校准时人机比对的依据。
- **rubric 是判据，不是提示词**。它定义"什么算对"，不能写成"请你写得准确一点"这种对 skill 的指令。

---

# Part A · 业务汇报助手 Rubric

## 2. 维度总览

| # | 维度 | 一句话判据 | 对应结构环节 | 权重 | 类型 |
|---|------|-----------|-------------|------|------|
| 1 | **数据准确性** | 每个数字/结论都能回溯到源数据，且计算/口径正确 | Step1/3 分离 + Step4 验证 + `source_ref` | **0.40** | 主维度（最易崩） |
| 2 | **完整性** | `required_sections` 全部覆盖，且关键 findings 无遗漏 | `report_spec` + Verifier 逐项核对 | 0.25 | 结构化（好托底） |
| 3 | **洞察质量** | 有真判断（归因/风险/下一步），不是数据复读 | Insight subagent + facts 历史基线 | 0.25 | 判断力 |
| 4 | **简洁性** | 无冗余、不堆术语、长度匹配受众 | `audience_profiles` | **0.10** | **反向维度（防 hack）** |

> 权重设计意图：数据准确性给 0.40，因为它是这个产品的生命线，也最容易崩；简洁性只给 0.10 但**必须存在**——它的作用不是拉高分，而是**堵住"靠堆砌信息骗高分"这条路**（见 §4 gate 里的一票否决）。

---

## 3. 各维度评分标准（1–5 锚点 + 正反例）

> 打分约定：整数 1–5。判据从上往下检查，**命中最严重的扣分项就落在对应档，不因其他方面好而回补**（准确性尤其如此——编一个数字，不能因为文笔好而给高分）。

### 维度 1 · 数据准确性（权重 0.40）

**判据**：报告中每一个数字、比较、结论，都必须①能回溯到某条 finding（带 `source_ref`），②计算和口径正确（同比/环比/vs目标算法对、指标定义与 `metric_definitions` 一致），③没有 findings 里不存在的"新数字"。

| 分 | 锚点（可观察表现） |
|----|-------------------|
| **5** | 所有数字/结论均可回溯到 finding；抽查计算与口径全部正确；无任何编造数字。 |
| **4** | 可回溯性完整、口径正确；仅有 1 处非关键的引用标记缺失或表述模糊，不影响结论。 |
| **3** | 主体数据可回溯，但有 1 处**关键数字无出处**，或 1 处**口径/计算错误**（如同比算成环比）。 |
| **2** | 出现 ≥1 个**编造数字**（findings 里不存在），或多处口径错误，核心结论的可信度受损。 |
| **1** | 大量数字无法回溯或明显编造；报告的数据基础整体不可信。 |

- ✅ **正例**："本月 ARR 达 ¥1,240 万，环比增长 8.3%[F-012]；增长主要由新增客户贡献（+¥95 万[F-014]），留存部分基本持平[F-015]。" —— 每个数字带 finding 引用，环比算法与口径正确，归因拆分也各有出处。
- ❌ **反例（编造）**："本月业绩表现强劲，ARR 突破 1,200 万，同比大幅提升约 30%，客户满意度也显著上升。" —— "约 30%"和"客户满意度上升"在 findings 里都不存在（编造）；"同比"可能与实际口径不符；无任何引用标记。**这类必须落到 2 分及以下**，即便文笔流畅。
- ❌ **反例（口径错）**：把"期初客户数"当分母算成 churn，与 `metric_definitions.churn_rate` 定义不符 → 落 3 分。

**区分度检查**：4 vs 3 的界线 = "缺的是关键数字/口径，还是无关紧要的标记"；3 vs 2 的界线 = "是引用/计算瑕疵，还是凭空编造"。**编造 = 2 分封顶，这是硬红线。**

---

### 维度 2 · 完整性（权重 0.25）

**判据**：`report_spec.required_sections` 里列出的段落**全部出现且非空占位**；且 Verifier 认定的"关键 findings"没有被遗漏（重要的异常/风险没被写进报告 = 漏报）。

| 分 | 锚点 |
|----|------|
| **5** | required_sections 全覆盖且每段有实质内容；无关键 finding 遗漏。 |
| **4** | 段落全覆盖；有 1 个次要 finding 未被提及，不影响整体判断。 |
| **3** | 缺 1 个 required section，**或**遗漏了 1 个关键 finding（如重大异常未写）。 |
| **2** | 缺 ≥2 个 required section，或遗漏多个关键 finding，报告不完整到影响决策。 |
| **1** | 大面积缺段，报告不成形。 |

- ✅ **正例**：月报 required_sections = [业绩概览, 关键指标, 异常与归因, 风险, 下一步]，五段齐全，且把 findings 里"华东区环比下滑 15%"的异常写进了"异常与归因"。
- ❌ **反例**：五段写了四段，"风险"段落缺失；同时 findings 里标了"现金跑道仅剩 5 个月"却没出现在报告里 → 缺段 + 漏报，落 2 分。

**区分度检查**：完整性只看"该有的有没有、关键的漏没漏"，**不评价写得好不好**（那是洞察/简洁维度的事）。避免维度间判据重叠。

---

### 维度 3 · 洞察质量（权重 0.25）

**判据**：报告在陈述数据之外，提供了**基于 findings 的判断**——至少包含：①对重要变化的**归因**（为什么会这样，且归因有数据支撑），②**风险/异常**的识别，③可执行的**下一步/建议**。判断必须落在 findings 内，不能凭空发挥。

| 分 | 锚点 |
|----|------|
| **5** | 归因具体且有数据支撑、风险识别到位、下一步可执行；提供了数据本身不直接显示、但由多条 findings 推出的非平凡判断。 |
| **4** | 有归因、风险、下一步，但其中 1 项较泛或支撑略弱。 |
| **3** | 有判断但偏表面（如只复述"增长了"却不解释为何、下一步空泛如"继续努力"）。 |
| **2** | 基本是**数据复读**，几乎无归因/风险/建议。 |
| **1** | 纯数字罗列，无任何判断成分。 |

- ✅ **正例**："环比增长虽达 8.3%，但拆分后新增贡献占 92%、留存持平[F-014/015]，增长质量依赖持续获客；叠加华东区连续两月下滑[F-020]，建议下月优先排查华东渠道并加固留存，否则获客成本上升时增长将失速。" —— 归因（拆分新增/留存）、风险（依赖获客+区域下滑）、下一步（排查+加固）俱全，且都挂在 findings 上。
- ❌ **反例（复读）**："本月 ARR 增长 8.3%，客户数增加，各项指标向好，请继续保持。" —— 只复述数据，无归因无风险，"下一步"空泛 → 落 2 分。
- ❌ **反例（越界发挥）**："增长说明我们的品牌战略非常成功。" —— "品牌战略成功"在 findings 里没有任何支撑，属于凭空归因 → 这条同时也会拉低**数据准确性**（无出处的结论）。

**区分度检查**：5 vs 4 = "是否提供了非平凡、需要综合多条 findings 才能得出的判断"；3 vs 2 = "是否有归因/风险/建议这三要素中的实质内容"。**注意越界发挥要跨维度扣分**（洞察 + 准确性双降），防止"编一个漂亮判断"骗洞察分。

---

### 维度 4 · 简洁性（权重 0.10，反向维度）

**判据**：报告无冗余（无重复表述、无与结论无关的数据堆砌、无空话套话/术语堆砌），且**长度匹配 `audience_profiles` 设定**（exec 简洁、team 详细）。**这是防 reward hacking 的反向闸门。**

| 分 | 锚点 |
|----|------|
| **5** | 无冗余，每段都服务于结论；长度与受众设定匹配。 |
| **4** | 基本精炼，有个别可删句子。 |
| **3** | 存在一定冗余或轻度术语堆砌，或长度偏离受众设定。 |
| **2** | 明显冗长/大量套话术语堆砌（"数据驱动""闭环""赋能"式堆词），或严重偏离受众长度设定。 |
| **1** | 充斥空话，信息密度极低。 |

- ✅ **正例**（exec 受众）：三段共 ~200 字，直击业绩、风险、决策项，无一句废话。
- ❌ **反例（典型 hack 症状）**："本报告基于数据驱动的分析方法论，通过多维度、全链路的深度洞察，构建了业务增长的闭环认知……" —— 满是 rubric 关键词和术语、信息量趋近于零 → 落 2 分。**这正是 Optimizer 可能学出来的讨好行为，simplicity 维度就是用来把它拽下来的。**

**区分度检查**：简洁性只惩罚"多而空"，**不奖励"短"**——为了短而漏内容，扣的是完整性，不是在这里加分。两个维度反向拉扯，才能逼出"既全又精"。

---

## 4. 打分门槛与 gate（供 Optimizer 的验证环节使用）

一个 skill 版本要被采纳/交付，除了看加权总分，还有**硬门槛**（防止按下葫芦浮起瓢、防 hack）：

- **红线一票否决**：数据准确性 < 3（即出现关键编造/口径错）→ 该 case 直接判不合格，无论总分多高。数据可信是这个产品的存在前提。
- **多目标不许拆东墙**：候选版本被采纳的条件是"**目标维度涨、且其它维度都不塌**"（任一维度较上版跌超过阈值即否决），分维度看，不看总分平均。
- **反向维度守门**：简洁性 < 3 触发人工复检——大概率是 Optimizer 开始靠堆砌骗分。

---

## 5. 聚合与阈值（`rubric` schema）

```yaml
rubric:
  product: report-assistant
  version: v0
  dimensions:
    - name: 数据准确性
      scale: 1-5
      weight: 0.40
      criteria: "每个数字/结论可回溯到 finding(带 source_ref)；口径与计算正确；无编造数字。"
      hard_floor: 3                 # 红线：低于此，该 case 判不合格
      positive_example: "本月 ARR ¥1,240 万，环比 +8.3%[F-012]，增长由新增贡献[F-014]…"
      negative_example: "ARR 突破 1200 万，同比大幅提升约 30%，满意度显著上升。(无出处/疑编造)"
    - name: 完整性
      scale: 1-5
      weight: 0.25
      criteria: "required_sections 全覆盖且非空；关键 finding 无遗漏。"
      positive_example: "五段齐全，且写入了华东区环比下滑异常。"
      negative_example: "缺'风险'段，且遗漏'现金跑道仅剩5个月'。"
    - name: 洞察质量
      scale: 1-5
      weight: 0.25
      criteria: "含归因/风险/下一步三要素，且判断落在 findings 内、非数据复读。"
      positive_example: "拆分新增vs留存，识别华东下滑风险，给出排查+加固建议。"
      negative_example: "各项指标向好，请继续保持。(纯复读，无判断)"
    - name: 简洁性                   # 反向维度：防 reward hacking
      scale: 1-5
      weight: 0.10
      criteria: "无冗余、不堆术语；长度匹配 audience_profiles。"
      positive_example: "exec 版 ~200 字，直击业绩/风险/决策项。"
      negative_example: "满篇'数据驱动/闭环/赋能'术语，信息量趋零。"
  aggregate: weighted_avg
  gates:
    - "任一 case 数据准确性 < 3 → 该 case 不合格（红线）"
    - "候选版本采纳条件：目标维度↑ 且 其它维度均不塌（分维度看）"
    - "简洁性 < 3 → 触发人工复检（疑似 reward hacking）"
  target:
    数据准确性: ">=4.2"
    完整性: ">=4.0"
    洞察质量: ">=3.8"
    简洁性: ">=3.5"
    overall: ">=4.0"
    judge_human_agreement: ">=0.85"   # meta-eval 门槛，见 §6
```

> 阈值是 v0 的初始设定，应在拿到首批人工标注、看到分数分布后校准。**洞察质量目标定得相对低（3.8）是刻意的**——它最主观、最难，硬拉高目标反而诱导 hack。

---

## 6. Judge 校准（meta-eval）—— 不做这一步，上面全是空中楼阁

rubric 写完不等于能用。**必须验证 judge 按这份 rubric 打的分，和人的判断一致。** 流程：

1. **准备校准集**：从数据集里抽 **30–50 条** trace，覆盖高/中/低质量分布，**由业务专家按同一份 rubric 人工打分**（分维度）。这批是 `eval_record.human_label`。
2. **让 judge 打同一批**，逐维度比对。
3. **算一致率**：
   - 分维度算 judge 与人工的一致性（如 ±1 分内视为一致的比例，或加权 kappa）。
   - **门槛：整体一致率 ≥ 0.85 才算 judge 可用**（`judge_human_agreement`）。
4. **不达标 → 改的是 rubric/judge，不是 skill**：
   - 看**分歧集中在哪个维度**——通常是那个维度锚点太模糊，或正反例不够典型。回去把锚点写细、补反例。
   - 反复迭代 rubric 描述，直到 judge 和人对齐。
5. **持续监控**：优化过程中每 K 轮抽样人工抽检。出现"test 分涨但人工觉得变差"= judge 被 hack，回到第 4 步修 rubric。

> 顺序铁律：**judge 没校准到 ≥0.85，不许开 Optimizer**。否则你只是在拿预算优化一个坏裁判。

## 7. Judge Prompt（骨架，配合本 rubric 使用）

```
你是业务汇报质量评审。依据下面的 rubric，对给定报告逐维度打分（1–5 整数）。

【硬规则】
- 逐维度独立打分，不要给总分（总分由系统按权重计算）。
- 每个分数必须附：① 分档理由；② 证据定位（引用报告/trace 中的具体片段）。
- 数据准确性：凡出现无法回溯到 finding 的数字/结论，或口径/计算错误，
  按 rubric 落 3 分及以下；出现编造数字，2 分封顶——不因文笔好回补。
- 不评价 rubric 之外的东西（如排版美观），只按四维度判据打分。

【输入】
rubric: {四维度锚点 + 正反例}
report_spec: {required_sections, audience, ...}
findings: [...]        # 用于核对可回溯性
report: {...}          # 被评报告
trace: {...}           # 完整执行过程

【输出（结构化）】
scores: { 数据准确性, 完整性, 洞察质量, 简洁性 }
reasoning: { 每维度: "分档理由 + 引用的证据片段" }
flagged: [ 命中红线/疑似hack 的具体问题 ]
```

## 8. 数据集要求（rubric 的另一半）

rubric 定义"什么是好"，数据集决定"在什么样本上比好坏"。二者都是专家资产，缺一不可：

- **切分**：train / dev / test 三分。**test（held-out）在优化全程不可见**，只用于防过拟合验证。
- **覆盖真实分布**：不同报告类型（周报/月报/经营简报/项目进展）、不同受众（exec/team）、不同数据规模都要有。
- **必须含硬 case**：数据有缺口的、有异常值的、口径易混的、findings 之间存在矛盾的——**这些"硬骨头"才是区分 skill 好坏的地方**，全是简单样本的数据集优化出来的 skill 一上线就崩。
- **校准子集**：其中 30–50 条带人工标注（§6 用）。

---

# Part B · Rubric 编写方法论（另外 3 个产品照着套）

## 9. 五步法：从产品到可用 rubric

1. **定维度（3–5 个）**：这个产品"好"体现在哪几件事？直接复用 v0 结构文档里已经列过的质量维度——**rubric 维度必须与结构维度一一对应**。**必须含 ≥1 个反向维度**（简洁/不越权/不臆断/不虚报）。
2. **给每维度写 1–5 锚点**：从"5 分什么样"和"最该扣分的 2 分什么样"两头往中间填。相邻档差别要能让两个人打出同分。
3. **配正反例，反例优先**：每维度至少 1 正 1 反。反例专挑"看着还行其实要扣"的边界样本——这是区分度来源。
4. **设 gate 和红线**：这个产品有没有"一票否决"的硬底线（如经营分析的数字准确性）？多维度之间会不会拆东墙补西墙？写进 gate。
5. **定聚合、阈值、校准门槛**：权重（生命线维度给高权重、反向维度给低权重但必存在）、target、`judge_human_agreement ≥ 0.85`。

然后走 §10 检查清单，通过才算完成。

## 10. Rubric 验收清单（Definition of Done）

- [ ] 3–5 个维度，且**每个都能对应到 v0 结构里的一个环节**。
- [ ] **至少一个反向维度**，用于防 reward hacking。
- [ ] 每维度都有 1–5 **锚点**，相邻档可区分（不是"好/较好/一般"这种空档）。
- [ ] 每维度 ≥1 正例 + ≥1 反例，**反例是边界/易混样本**。
- [ ] 判据全部**可判定**（能指着 trace 说清扣分点，无"高质量"类空词）。
- [ ] 设了**红线/gate**（一票否决项 + 多维度不许互相牺牲）。
- [ ] 有权重、target 阈值、`judge_human_agreement ≥ 0.85`。
- [ ] 准备了 **30–50 条人工标注校准集**，且数据集**含硬 case**。
- [ ] 配了 Judge prompt，要求分维度打分 + 理由 + 证据定位。

## 11. 三个产品的针对性提示

> 起点提示，非定稿。每个产品负责人走完整五步法。

- **商业分析平台**：主维度是**归因正确性 / 论证严谨性**。"看着有道理其实站不住"是最大风险，judge 和 rubric 要能查**论证链是否被证据支撑、有无跳步**，而不只是查数字。反向维度：**不过度推断**（把相关当因果、样本外泛化要扣分）。红线：核心结论无证据支撑。
- **项目管理平台**：主维度是**状态一致性 / 完整性**（任务、里程碑、风险、阻塞的状态准确且不漏）。反向维度：**不虚报进度**（把"进行中"写成"已完成"要重扣）。gate：与上一期状态的对齐核查。
- **公司经营分析**：与业务汇报助手最接近但**准确性和口径一致性门槛最高**（现金流、经营指标错一个后果严重）。红线阈值应比业务汇报助手更严（如准确性 hard_floor 提到 4），考虑双重验证 + 更严格的 `metric_definitions` 核对。

---

## 附录 · 常见 Rubric 错误清单

- ❌ 维度判据用"高质量/专业/清晰"等无法核对的词 → judge 各打各的，一致率永远上不去。
- ❌ 只给单一总分让 judge 自由发挥 → Optimizer 无从归因，且会牺牲难维度换易维度。
- ❌ 只有正例没有反例 → 没有区分度，边界样本全靠 judge 猜。
- ❌ 没有反向维度 → Optimizer 一定会学出"又长又满是关键词"的讨好式输出。
- ❌ 跳过 judge 校准直接开优化 → 在优化一个坏裁判，越优化越偏。
- ❌ 数据集全是简单样本 → 分数好看，skill 一遇真实硬 case 就崩。
- ❌ target 阈值拍脑袋定死、不看首批分数分布 → 要么永远达不到，要么一开始就"达标"。
- ❌ rubric 写成对 skill 的指令（"请写准确一点"）而非判据（"准确=每个数字可回溯"）→ 混淆了"评"和"做"。

---

*本文档与 `业务汇报助手_v0结构设计文档.md` 构成一对：结构定上限，rubric 定义"什么是好"并守住优化不跑偏。两者都是人工专家资产，是平台里最不能外包、最该先做对的部分。*
