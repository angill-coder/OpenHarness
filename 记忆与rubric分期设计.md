# 记忆与 Rubric 分期设计（专家反馈的分流）

> 讨论定稿 2026-07-17。回答:专家参与打分/给反馈时,哪些改 rubric、哪些进 memory,怎么区分与实现。
> 现状:app 里**还没有** memory 层、用户概念、L2/L3 优化——本文是这些的 spec 起点。配套 `HANDOFF.md`、`调研洞察汇报助手_Rubric落地文档.md`。

---

## 1. 核心区分:生产 vs 评判(为什么 rubric ≠ skill)

- **skill(含 memory)= 生产者**:结构 + directives + 领域知识 + few-shots,负责**产出**报告。
- **rubric = 评分标准**:judge 拿它 + ground_truth 给产出**打分**,负责**评判**。

两者内容**刻意对齐**(每个 rubric 失分点配一个 directive),但角色相反、不能合并:

- **指令 ≠ 结果**:skill 写"挂出处"不等于出处真对得上(可能编/错配);写"禁不是而是"不等于真没有。LLM 不完美执行、会幻觉、会自我感觉良好。**只有独立的 rubric+judge 对着 ground_truth 才能量出"到底做到没"。**
- **自己不能考自己**:若 skill 与 rubric 是一个东西,"声称合规"就=高分,分数失去意义。分开 → skill 被一个它改不了的、稳定的外部标准评判 → 分数可信、优化闭环("瞄准→独立量→按失败改")才成立。
- **rubric 独有"评分机器"**(skill 没有、也不该写进去):权重、1–5 锚点、红线/gate、目标阈值、校准门槛、对 ground_truth 的编造/漏答判定、反向维度。这些是"怎么量",不是"怎么写"。

一句话:**质量判据抄进 skill(让它瞄准)+ 保留独立 rubric 测量结果(因为指令≠结果)——对齐是为瞄准,分开是为可信。**

---

## 2. 专家反馈的三个去处(不是两个)

| 去处 | 是什么 | 影响谁 | 治理 |
|---|---|---|---|
| **① Rubric(共享标准)** | "什么算好":锚点/checks/权重/红线 | judge(所有人的评分) | 共享、版本化(r1/r2)、**owner 把关**、校准驱动 |
| **② Skill memory(共享上下文)** | 领域事实/规则,生成时套用:如"本公司口径=累计前台时长" | skill 的生成(所有人) | 共享、可累积(L3) |
| **③ 个人 memory(个体上下文)** | 某用户的偏好/语境,**不该成为全局标准** | 只个性化**该用户的 skill 生成** | 按用户隔离,永不碰共享 rubric/judge |

判定一条反馈去哪的三个问题:①该怎么评 / 该知道什么 / 我个人怎么看 → rubric / skill-memory / 个人;②能泛化吗(多人认同、跨case成立)→ 共享 vs 个体;③改 judge / 改 skill / 改个人镜片。

**隔离铁律**:个人 memory 绝不泄进 rubric,judge 不对不同用户打不同分——否则共享标准碎裂,平台失去意义。

---

## 3. 分期实现:用"时间分期"替代"实时分类"

难点是"一条反馈实时判属于哪类"不可靠(人会把个人口味当普遍真理)。解法是**分期**:

### 第一期 · 定标准（memory 开关**关**）
- 目标:把 rubric 调对、judge 校准到 ≥0.85。
- 所有专家反馈都当 rubric 用(共享)。
- **闸门(客观)**:`judge↔人工 一致率 ≥0.85 且分维度达标 → rubric 冻结 → 自动解锁下一期`。在 rubric 没校准前开个人 memory 无意义。
- **单专家防偏**:没条件多专家轮流标时,用**校准集(覆盖高/中/低质量的真实+坏样本)**替代——专家对着**具体例子**表态,压制抽象个人口味;可用"事后抽检/owner review"做顺序交叉,不必同时多人。

### 第二期 · 个性化（memory 开关**开**,每用户独立）
- rubric 已冻结、是既定标准 → 用户此时加的东西**按定义就是个人 memory**(分流变 trivial)。
- 个人 memory **只个性化该用户的 skill 生成**(如"我偏好更短""我这条线口径用X"),**judge 仍按冻结 rubric 对所有人一致**。
- **逃生通道**:若用户发现的是"标准本身错了"(系统性、多人撞到)→ 上报 owner → 重开 rubric 评审 → 再冻结。即"个人 memory 为默认,rubric 冻结但可上报重开"。

### 用两个一致率做数据兜底(判"该改 rubric 还是个人")
- judge↔人低、但**人↔人高**(专家彼此认同、只和 judge 不一致)→ 系统性共识分歧 → **改 rubric**。
- **人↔人也低**(专家彼此都不一样)→ 主观/因人而异 → 不是 rubric 价值问题,要么锚点太模糊(消歧式改 rubric),要么进个人 memory。

---

## 4. 三层优化 + memory/few-shot 各装什么

skill 的可优化槽位对应三层(MVP 只做 L1,L2/L3 槽位留好未实现):

| 层 | 槽位 | 装什么 | 状态 |
|---|---|---|---|
| **L1** | `instructions.directives` | 指令开关(22 质量 directive) | ✅ 已实现在跑 |
| **L2** | `few_shots` | **范例**(输入→输出):冲突处理范例、标杆摘要节选、反例→改写对。示范比规则更能教会难描述的分寸(提炼 vs 罗列) | 槽位留好,未实现 |
| **L3** | `memory_content` | `config`(模板/受众,已有一点)、`facts`(领域事实,空)、`learned_rules`(从失败学到的规则,空) | 槽位留好,未实现 |

**个人 memory = 按用户隔离的一份 `facts`/`learned_rules`**,叠在共享 skill 之上,只在该用户跑 skill 时生效于生成,不进共享层、不碰 rubric。

### 4.1 L2/L3 何时填(按代价分层、平台期触发升级)

原则:**L1(改指令,最便宜)→ L2(加范例)→ L3(改记忆)→ 结构(最贵)**。便宜的先用尽,撞平台期还差,才升级。填哪层由**失败的性质**决定:

- **填 L2 few_shots**:L1 已撞平台期、某维仍不达标,且失败是**"指令说了但做不稳 / 分寸难用文字讲清"**——示范比规则管用。例:insight 卡在 3–4(懂"提炼非罗列"却拿不准分寸)→ 加一个"5家案例→提炼成某模式"的范例;反复冒"不是,而是"→ 加"反例→改写"对。**信号:会做但做不稳的分寸 → L2。**
- **填 L3 memory**:失败是**"缺领域知识/事实"**,指令和例子都修不了(缺的是 fact)。
  - `facts`:反复用错口径,因为**不知道**"本公司口径=X"→ 给它这条事实。
  - `learned_rules`:跨 case 反复同类错(如"问卷不查样本代表性就下结论")→ 固化成一条规则。
  - **信号:缺一条事实/知识、或跨case反复该固化 → L3。**

**两个填充时机**(别只想着优化器自动填):
1. **人工预置(开局就放)**:已知的领域事实/标杆例子,像 ground_truth 一样开局手动放进 `memory.facts` / `few_shots`——不必等优化器去"发现"。**现在就能做**(前提:是**通用**约定,不是 case 特定的——case 特定事实归各 case 的 ground_truth)。
2. **优化器平台期自动升级**:L1 用尽、失败还在,诊断出"分寸问题→L2 / 知识缺失→L3"。**未实现**;现平台期诊断只分"指令级 vs 结构性",把它细分成 L2/L3 自动判别是待办。

**前提**:L2/L3 同 L1,都在校准过的 judge(≥0.85)下做;顺序严格 L1→L2→L3→结构。

---

## 5. 待实现清单(本文是 spec)

- app 引入**用户概念** + 每用户 **memory 开关**(绑定 rubric 校准闸门:未过关=关)。
- 个人 memory 存储(按用户隔离的 facts/learned_rules)+ 生成时注入该用户 memory。
- 第二期的 **rubric 上报重开**通道(个人期发现系统性标准问题→owner 评审)。
- L2/L3 优化器动作(加/改 few-shot、memory),在 L1 平台期后触发。
- 两个一致率的统计(judge↔人、人↔人)作为 rubric-vs-个人 的数据兜底。
