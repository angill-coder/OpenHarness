# 业务汇报助手 · v0 结构设计文档（兼「结构设计模板」）

> **文档定位**：本文件有两个用途，合二为一。
> 1. **v0 起点**：业务汇报助手这个 skill 的初始结构（执行流程 / subagent 拆分 / memory schema），可直接填入 Skill Artifact 的 `structure` 字段，作为优化闭环的起跑线。
> 2. **结构设计模板**：把这套结构背后的**设计方法**抽象成可复用的五步法 + 空白模板 + 通用模式库，供另外 3 个产品（商业分析平台、项目管理平台、公司经营分析）照着套。
>
> **读者**：负责各 agent 产品的开发同学。你不需要很强的工程背景，但你必须**读懂并认同这里每一条"为什么这样拆"的理由**——因为结构决定了 skill 质量的上限，是后续自动优化再强也够不到的天花板。
>
> 维护者：平台/架构负责人 · 版本：v0 · 最后更新：2026-07-05

---

## 0. 开始之前：三条铁律

在动手设计任何一个产品的 v0 结构前，先记住三句话。它们是这份文档所有细节背后的第一性原理：

1. **结构定上限，先由人设对。** 流程缺一步、subagent 拆错、memory 混层，后面无论怎么自动调 prompt 都撞天花板。v0 结构不是"随便搭个能跑的"，而是**一次性的专家动作**。平台的自动优化（Optimizer）在 MVP 阶段**只动指令文本 / few-shot / memory 内容，不动结构**。
2. **把"算数据"和"写文字"彻底分开。** 这是所有涉及"用数据说话"的产品里，抬高上限**最重要的一刀**。让同一个上下文既分析又写作，模型会一边写一边顺口编数字——数据准确性天然崩。
3. **验证必须独立且对抗。** 让生成者自己检查自己，等于没检查。凡是"准确性/完整性"要求高的产品，都需要一个**独立上下文、任务是"挑错"而非"确认"**的 Verifier 回路。

---

## 1. 背景：这个结构在整个平台里的位置

我们做的是一个 **eval 驱动的 skill 自动优化平台**。核心闭环：

```
Dataset + Rubric（下属提供）
        │
        ▼
Runner → Judge → Failure Clustering → Optimizer → 新 skill 版本
   ▲                                                    │
   └───────────────── Skill Artifact Store（版本化）─────┘
        │
        ▼
Regression Dashboard（看分数是否稳定爬升、是否过拟合）
```

一个 skill 被抽象成**可版本化的 artifact**：

```
skill = { structure（结构，人工设定）, instructions, few_shots, memory_content }
                    └── 本文档负责的部分 ──┘   └──── 平台自动优化的部分 ────┘
```

**本文档只负责 `structure`。** 这是"定上限"的活，也是最该先做对、最不能交给自动搜索碰运气的部分。`structure` 一旦设好，就作为 v0 填进 Artifact Store，Optimizer 在它之上迭代 instructions / few_shots / memory_content。

> 什么时候才动结构？—— 当 prompt 调优明显停在 rubric 目标之下、且失败分析显示问题是**架构性的**（比如"它做不到 X，因为流程里根本没有检索这一步"），才触发结构级优化。**由信号触发，不在 v0 赌。**

---

# Part A · 业务汇报助手 v0 结构

## 2.1 这个产品到底在做什么

**核心任务**：输入零散的业务数据 / 上下文 → 输出一份**可信、完整、有洞察**的业务汇报（周报 / 月报 / 经营简报 / 项目进展）。

它的质量上限由四件事决定，**整个结构就是围绕这四件事设计的**：

| # | 质量维度 | 说明 | 特点 |
|---|---------|------|------|
| 1 | **数据准确性** | 每个数字 / 结论都能回溯到源数据 | 最容易崩，最需要结构兜底 |
| 2 | **完整性** | 该有的段落都有 | 结构化任务，好托底 |
| 3 | **洞察质量** | 有真洞察（风险、归因、下一步），不是数据复读 | 依赖判断力 + 历史基线 |
| 4 | **简洁性** | 不冗长、不堆术语 | 反向维度，用来防 reward hacking |

## 2.2 执行流程（Flow）

**关键设计原则**：把"算数据"和"写报告"彻底分开（铁律 #2）。分开之后，写作阶段**只能引用已算好的、带出处的 findings**，无权自由生成数字。

```
┌─────────────────────────────────────────────────────────────────┐
│ STEP 0  Intake & Scoping                                          │
│  确定：报告类型 / 受众 / 周期 / 可用数据源。缺什么先标记出来          │
│  产出：report_spec { type, audience, period, required_sections }  │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 1  Data Ingestion & Validation      [Data Analyst subagent] │
│  解析数据 → 校验(缺口/异常/口径) → 计算指标(同比/环比/vs目标)         │
│  ★只出数字，不写文章★                                              │
│  产出：findings[]  每条带 { value, source_ref, computation }      │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2  Insight Extraction               [Insight subagent]      │
│  从 findings 里挑"什么重要、为什么、有何风险、下一步"                 │
│  ★只引用 findings 里的数字，不能引入新数字★                         │
│  产出：insights[]  带 { claim, based_on: [finding_id], severity } │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3  Narrative Composition            [Writer subagent]       │
│  按 memory 里的模板，把 findings + insights 组织成报告              │
│  每个论断携带 [finding_id] 引用标记                                 │
│  产出：draft_report                                               │
└───────────────────────────────┬─────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4  Verification（独立、对抗性）      [Verifier subagent]      │
│  逐句检查：① 每个数字/论断都能回溯到 finding 吗？（抓编造）           │
│           ② required_sections 齐了吗？（抓遗漏）                    │
│           ③ 有没有 findings 没提到但很关键的？（抓漏报）             │
│  产出：verification_report { pass / fail, 逐条问题 }               │
└───────────────────────────────┬─────────────────────────────────┘
                        ┌────────┴────────┐
                    pass │            fail │ （退回 Step 3 重写，最多 N 次）
                        ▼                  ▼
┌─────────────────────────────────┐   回到 Writer，带着问题重写
│ STEP 5  Format & Deliver          │
│  按受众套格式（exec 精简 / team 详细）│
└─────────────────────────────────┘
```

**为什么这个流程决定上限：**

- **Step 1 ↔ Step 3 分离**：从架构上封死了"编数字"这条最大的失分路径——写作层拿不到"自由生成数字"的权力。
- **Step 4 独立验证回路**：是数据准确性维度的天花板保障。**必须是独立上下文 + 对抗性**（任务是"挑错"而非"确认"），否则等于自己检查自己，形同虚设。
- **Step 0 的 report_spec**：让"完整性"变得**可判定**——`required_sections` 是一张清单，Verifier 逐项打勾。

## 2.3 Subagent 拆分

拆分依据是**职责隔离 + 上下文聚焦 + 让验证独立**，不是为拆而拆。

| Subagent | 单一职责 | 为什么单独拆（抬上限的理由） |
|----------|---------|---------------------------|
| **Data Analyst** | 算数、校验、给数字配出处，输出结构化 findings | 把"精确计算"和"自由写作"隔离，从根上防编造；上下文只装数据 |
| **Insight** | 判断重要性 / 归因 / 风险，只在 findings 内推理 | "什么值得说"是独立判断力，与"算得准""写得好"是不同能力 |
| **Writer** | 组织语言、套模板，携带引用标记 | 让写作上下文只关心表达，不被计算细节干扰 |
| **Verifier**（独立） | 对抗性查错（编造 / 遗漏），不参与生成 | **★最关键★** 独立上下文才能真正挑错；这是数据准确性的兜底 |

**一个 v0 的取舍：先做这 4 个，别更多。** 比如"数据源连接器"暂时不拆成 subagent，先当 Data Analyst 的内部步骤——subagent 拆太细会增加编排复杂度和 token 成本，v0 先验证主干。要不要进一步拆，等 Optimizer 撞平台期、失败模式指向具体环节时再说。

## 2.4 Memory Schema

Memory 分三类，因为它们的**写入者、更新频率、可信度完全不同**——混在一起是最常见的设计错误。

```yaml
memory:

  # ① 配置型 memory —— 稳定，人工/初始化写入，决定"这个报告长什么样"
  config:
    report_templates:              # 各报告类型的结构模板
      monthly_biz_review:
        required_sections: [业绩概览, 关键指标, 异常与归因, 风险, 下一步]
        audience: exec
        tone: concise
    metric_definitions:            # 指标口径词典（消除"同一个词不同算法"）
      churn_rate: "当期流失客户数 / 期初客户数，含定义与 SQL 口径"
      arr: "..."
    audience_profiles:             # 受众偏好
      exec: { length: short, focus: [风险, 决策项], detail_level: low }
      team: { length: long, detail_level: high }

  # ② 事实型 memory —— 累积型，每次 run 追加，支撑"同比/环比"
  facts:
    historical_baselines:          # 历史各期指标，Data Analyst 做对比要用
      - { period: 2026-05, arr: ..., churn_rate: ..., source_ref: ... }
    prior_reports: [ 指向历史报告的引用 ]

  # ③ 习得型 memory —— 反馈/优化写入，是 Optimizer 和人工纠正的落脚点
  learned_rules:                   # ★这块正是优化闭环写入的地方★
    - rule: "经营月报必须包含现金流跑道（cash runway）"
      origin: 人工反馈 2026-06
    - rule: "增长类结论必须区分'新增'与'留存'贡献"
      origin: optimizer 从'归因笼统'失败模式提炼
    - known_data_quirks: [ "华东区数据延迟 1 天，月末需备注" ]
```

**为什么这样分层直接关系到上限和优化闭环：**

- **config** 决定个性化的天花板——模板、口径、受众偏好定得越准，输出越贴合真实需求。**这是人工资产。**
- **facts** 决定"能不能做对比分析"——没有历史基线，同比环比无从谈起，洞察维度直接封顶。
- **learned_rules** 是**优化闭环的物理落点**。人工反馈和 Optimizer 提炼的规则都沉淀在这里，让改进**可累积、可追溯、跨会话生效**，而不是每次重调 prompt。这一层设计好，平台"越用越好"才成立。

## 2.5 结构 ↔ 四维度自检表

这是验证"结构没有留下够不到的盲区"的关键一步。每个质量维度都必须能指向一个明确的结构对应物：

| 质量维度 | 结构对应物 |
|---------|-----------|
| **数据准确性** | Step 1/3 分离 + Step 4 独立验证 + findings 带 `source_ref`（最强保障） |
| **完整性** | `report_spec.required_sections` + Verifier 逐项核对 |
| **洞察质量** | Insight subagent 专职 + facts 提供历史对比基线 |
| **简洁性** | `audience_profiles` 控制长度 + 作为反向维度防堆砌 |

四个维度都有明确的结构对应物 → 说明 v0 结构没有留下"再怎么调 prompt 也够不到"的盲区。**这就是"结构定上限、且上限够高"的验证。**

## 2.6 填入 Skill Artifact

这套结构直接作为 `skill.structure` 的初始值：

```yaml
skill:
  id: report-assistant
  version: v0
  parent_version: null
  structure:                       # ← 本文档定义，MVP 里 Optimizer 不改
    flow: [Step0, Step1, Step2, Step3, Step4→loop, Step5]
    subagents: [DataAnalyst, Insight, Writer, Verifier]
    memory_schema: { config, facts, learned_rules }
  instructions: "..."              # ← 以下由 Optimizer 迭代
  few_shots: [...]
  memory_content: {...}
```

---

# Part B · 结构设计模板（另外 3 个产品照着套）

Part A 是一个填好的样例。Part B 把方法抽象出来，让**商业分析平台 / 项目管理平台 / 公司经营分析**照着走一遍。

## 3.1 五步法：从产品到 v0 结构

> 顺序不能乱。**先定"什么是好"，再谈怎么拆**。跳过第 1 步直接画流程图，是最常见的失败方式。

1. **写清核心任务**：一句话说明"输入什么 → 输出什么"。
2. **列出 3–5 个质量维度**：这个产品的输出，"好"体现在哪几件事上？至少要有一个**反向维度**（如简洁性 / 不越权 / 不臆断）用来防 reward hacking。这一步和 Rubric 是同一件事的两面。
3. **画执行流程**：把任务拆成有序的 Step。**凡涉及"用数据/事实说话"，就必须把"取证/计算"与"表达/生成"分成不同 Step。**
4. **定 subagent**：按"职责隔离 + 上下文聚焦 + 验证独立"把 Step 归到 subagent。v0 控制在 **3–5 个**。
5. **设计 memory schema**：按"配置型 / 事实型 / 习得型"三层分。确认 `learned_rules`（习得层）是优化闭环的写入口。

最后做 **3.5 的自检**：每个质量维度都能指回一个结构对应物，才算 v0 结构合格。

## 3.2 空白模板（复制这段去填）

```markdown
# <产品名> · v0 结构设计

## 1. 核心任务
输入：______  →  输出：______

## 2. 质量维度（3–5 个，含 ≥1 个反向维度）
| # | 维度 | 说明 | 最容易崩吗 |
|---|------|------|-----------|
| 1 |      |      |           |
| ... |    |      |           |

## 3. 执行流程（Flow）
STEP 0 ______  产出：______
STEP 1 ______  [__ subagent]  产出：______
...
STEP N 验证（独立、对抗）[Verifier]  产出：pass/fail + 逐条问题
（标出：哪一步取证/计算，哪一步表达/生成，二者是否分离）

## 4. Subagent 拆分（3–5 个）
| Subagent | 单一职责 | 为什么单独拆（抬上限的理由） |
|----------|---------|---------------------------|

## 5. Memory Schema
config:        # 稳定，人工写入
facts:         # 累积，每次 run 追加
learned_rules: # 反馈/优化写入 ← 优化闭环落点

## 6. 结构 ↔ 维度自检表
| 质量维度 | 结构对应物 |
|---------|-----------|
（每个维度都必须能填一行，填不出=有盲区，回去改结构）
```

## 3.3 通用结构模式库

这些是跨产品复用的"零件"。设计时按需取用：

- **生成/取证分离**：凡是输出要"用数据或事实背书"的产品，把"拿证据 + 计算"和"组织语言"拆成不同 Step / subagent。写作层只能引用带出处的证据。→ 直接抬高准确性上限。
- **独立对抗验证回路**：加一个只挑错、不生成的 Verifier，独立上下文，输出 pass/fail + 问题清单，fail 则带问题退回重写（限次数）。→ 准确性 / 完整性的兜底。
- **spec 驱动完整性**：在流程最前面产出一个 `spec`（含 `required_*` 清单），让"完整性"从模糊感觉变成可逐项打勾的清单。
- **三层 memory**：`config`（稳定人工资产）/ `facts`（累积事实，支撑对比与趋势）/ `learned_rules`（反馈与优化的落点）。三者写入者不同，务必分开。
- **受众/场景 profile**：把"给谁看、要多详细"抽成配置，而非写死在 prompt 里。→ 控制简洁性维度，也便于个性化。
- **v0 克制原则**：subagent 3–5 个封顶，不确定要不要拆的先当内部步骤。等失败信号指向它再拆。

## 3.4 三个产品的针对性提示

> 这些只是起点提示，不是定稿。每个产品的负责人仍要走完整的五步法。

- **商业分析平台**：输出更长链条的分析论证，"洞察质量"和"归因正确性"是主维度，也最容易"看着有道理其实站不住"。**Verifier 要额外查"论证链条是否被证据支撑、是否有跳步"**，而不只是查数字对不对。历史基线（facts）尤为关键。
- **项目管理平台**：偏结构化、状态驱动（任务 / 里程碑 / 风险 / 阻塞）。**完整性和状态一致性是主维度**，`spec` 驱动特别有效。可能需要一个"状态核对"性质的 Verifier（对齐上一期状态、检查有没有漏更的项）。反向维度可设为"不虚报进度"。
- **公司经营分析**：与业务汇报助手最接近，但**准确性和口径一致性要求最高**（涉及现金流、经营指标，错一个数字后果严重）。`metric_definitions` 口径词典必须极其严格，Verifier 的对抗性要拉满，考虑双重验证。

## 3.5 v0 结构验收清单（Definition of Done）

一份 v0 结构设计，满足以下全部才算完成、可提交：

- [ ] 核心任务一句话说清（输入 → 输出）。
- [ ] 有 3–5 个质量维度，且**至少一个是反向维度**。
- [ ] 流程里，"取证/计算"与"表达/生成"是**分离**的（若产品涉及用数据说话）。
- [ ] 有一个**独立、对抗性**的验证步骤。
- [ ] subagent 数量在 3–5 个，每个都有单一职责和"为什么拆"的理由。
- [ ] memory 按**三层**划分，`learned_rules` 层明确是优化闭环的写入口。
- [ ] **自检表里每个质量维度都能指向一个结构对应物**（这是最硬的一条——填不满就是有盲区）。

---

## 附录 A · Skill Artifact / Eval / Rubric Schema（平台骨架，供参考）

**Skill Artifact**（被版本化、被优化的东西）：

```yaml
skill:
  id: report-assistant
  version: v3
  parent_version: v2          # 追溯优化血缘
  # ↓ Phase 0 人工设定，MVP 里 optimizer 不改
  structure:
    flow: [...]               # 执行流程 / 步骤编排
    subagents: [...]          # subagent 拆分与职责
    memory_schema: {...}      # memory 的结构定义
  # ↓ MVP 里 optimizer 优化的部分
  instructions: "..."         # 主指令 / prompt
  few_shots: [...]            # 示例
  memory_content: {...}       # memory 里的实际内容
  changelog: "相对 v2：强化了数据引用要求，新增反例 2 条"
```

**Eval Record**（一条 trace 的评测结果）：

```yaml
eval_record:
  run_id: ...
  skill_version: v3
  dataset_split: dev          # train / dev / test
  case_id: ...
  input: {...}
  trace: {...}                # 完整执行过程，不只最终输出——聚类和归因都靠它
  output: {...}
  scores:                     # 按 rubric 分维度
    完整性: 4
    数据准确性: 2              # ← 低分维度是聚类的抓手
    结构清晰度: 5
  judge_reasoning: "..."      # 打分理由，给失败聚类和 optimizer 读
  human_label: {...}          # 仅校准样本有，用于 meta-eval
```

**Rubric**（专家资产 —— 平台成败的杠杆，另出专门文档）：

```yaml
rubric:
  product: report-assistant
  dimensions:
    - name: 数据准确性
      scale: 1-5
      criteria: "每个结论必须可回溯到输入数据……"   # 可判定，不能是'好/不好'
      positive_example: "..."      # 正例
      negative_example: "..."      # 反例（区分度的关键）
  aggregate: weighted_avg          # 维度如何汇总成总分
  target: { 数据准确性: ">=4", overall: ">=4.2" }   # 收敛阈值
```

## 附录 B · 常见错误清单

- ❌ 跳过质量维度直接画流程图 → 结构没有可对齐的目标，等于凭感觉搭。
- ❌ 让写作/生成层能自由产出数字或事实 → 准确性从架构上就崩了。
- ❌ Verifier 和生成者共享上下文 / 任务写成"确认无误" → 假验证。
- ❌ memory 三层混在一起 → 稳定配置被优化流程覆写，事实基线被当规则，越用越乱。
- ❌ v0 就拆 8+ 个 subagent 或想让平台自动搜结构 → 编排复杂、成本爆炸、归因崩溃。
- ❌ 自检表有维度填不出结构对应物却仍然提交 → 埋了一个 prompt 永远够不到的天花板。

---

*本文档是"结构定上限"这一原则的落地。Rubric 落地是配套的另一半（决定平台成败的杠杆），建议紧随其后产出。*
